[#!/bin/bash]

# Navigate to the home directory
cd ~

#Create a directory called "myDirectory" in your home folder.
mkdir myDirectory

# Navigate to the myDirectory directory
cd myDirectory

# Inside "myDirectory," create another directory named "secondDirectory."
mkdir secondDirectory

# Navigate to the secondDirectory directory
cd secondDirectory

# Within "secondDirectory," create a file named "myNotePaper."
touch myNotePaper

# Navigate back to "myDirectory"
cd ../

# Copy "myNotePaper" from "secondDirectory" to "myDirectory."
# Rename the copied file in "myDirectory" to "myOldNotePaper."
cp secondDirectory/myNotePaper myOldNotePaper 

# show the contents of "myDirectory"
ls

